import type { TConfig } from "commitsmile/index";

// Remove 'zod' or '@clack/prompts' - TConfig will not give you hints because he imports from '@clack/prompts' and 'zod'
// Ctrl + Space = Hints
const config: TConfig = {};

export default config;
